# Default API base URL baked into the production bundle at build time.
# Used when the user has not set a custom URL in the panel (localStorage).
# Omit for the local default: http://localhost:3000
VITE_API_BASE_URL=http://localhost:3000
